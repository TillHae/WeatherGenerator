healpix_level: 3
rope_2D: False

# Data streams
streams_directory: "./config/streams/era5_1deg/"
zarr_store: "zip"

training_config:
  num_mini_epochs: 8
  max_compute_budget_l6_steps: 512
  
wgtags:
  exp: "coarse_to_fine_multistage"
  stage: "L3"
  issue: null
